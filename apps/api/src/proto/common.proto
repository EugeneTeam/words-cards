syntax = 'proto3';

package proto;

message Empty {}

message Status {
  bool status = 1;
}

message Pagination {
  optional int32 limit = 1;
  optional int32 offset = 2;
}

message UserUuidAndPagination {
  string userUuid = 1;
  optional Pagination pagination = 2;
}
